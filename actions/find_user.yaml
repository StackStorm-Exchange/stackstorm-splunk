---
name: find_user
runner_type: python-script
description: Splunk retrieve user 
entry_point: find_user.py
parameters:
  instance:
    type: string
    description: "Splunk instance if not default to search (must be in config.yaml)"
    required: false
    position: 0
  user:
    type: string
    description: "The user search for and to return information on"
    required: true
    position: 1
