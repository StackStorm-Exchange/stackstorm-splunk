---
name: get_hec_token
runner_type: python-script
description: Splunk get HEC token
entry_point: get_hec_token.py
parameters:
  instance:
    type: string
    description: "Splunk instance if not default to search (must be in config.yaml)"
    required: false
    position: 0
