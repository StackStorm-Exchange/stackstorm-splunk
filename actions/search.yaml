---
name: search
runner_type: python-script
description: Splunk one-shot search
entry_point: search.py
parameters:
  host:
    type: string
    description: "Splunk instance to search (must be in config.yaml)"
    required: true
    position: 0
  query:
    type: string
    description: "Search query (e.g. 'search * | head 10')"
    required: true
    position: 1
