---
name: get_user
runner_type: python-script
description: Splunk retrieve user 
entry_point: get_user.py
parameters:
  instance:
    type: string
    description: "Splunk instance if not default to search (must be in config.yaml)"
    required: false
    position: 0
  user_name:
    type: string
    description: "The user to retrieve"
    required: true
    position: 1
